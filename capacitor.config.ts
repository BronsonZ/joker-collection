import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jokerbabies.app',
  appName: 'joker-collection',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
